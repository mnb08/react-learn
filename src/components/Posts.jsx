import React from 'react'
import shortid from 'shortid'

export const Posts = ({item}) => {
  return (
    // <div className='posts' key={shortid.generate()}>
    //     <img src={item.thumbnail} alt="" />
    //     <h2>{item.title}</h2>
    //     <p>{item.content}</p>
    // </div>
    <div>
      this is posts page
    </div>
  )
}
