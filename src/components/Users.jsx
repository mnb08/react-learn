import React from 'react'
import shortid from 'shortid'

export const Users = ({item, setDeleteId}) => {
    return (
        // <div key={shortid.generate()}>
        //     <h1> {item.firstname} </h1>
        //     <h3> {item.lastname} </h3>
        //     <h3> {item.email} </h3>
        //     <button
        //         onClick={ () => setDeleteId(item.id)}
        //     > x </button>
        // </div>
        <div>
            this is users page
        </div>
    )
}
